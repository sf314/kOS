// lib.stdlib.ks
// Standard functions

print "Standard Library".

// Maybe just use includes instead of loading imu, sensors by default
// copypath("0:imu.ks", "").
// run imu.ks.
// 
// copypath("0:science.ks", "").
// run science.ks.
